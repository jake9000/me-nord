# ME-NORD

1. Review README.md for foundational info about this repo. 
2. All writes happen in nord.css. nord.json, the compiled version, is created by invoking ../compile-marinara-json. this must be done before comitting any changes to git. 
3. If an element cannot be themed with a pure .css file, use ../me-themehelper to expose that element. Do not try to modify marinara engine directly. 